#include<iostream>
#include<string>
#include"tcp.h"
#include"udp.h"
using namespace std;
int mian() {
  init();
  cout << "Welcome to flick CLI" << endl;
  string input;
  
  while (true) {
    cin >> input;
    if (
  }
}
